# Hero / mood image

The README hero image lives at `docs/assets/orkoprox-hero.png`.

It is generated separately (image LLM); the generation prompt is maintained
outside the codebase. Drop the rendered PNG here and the README will pick it up.
